<section id="{{sectionId}}" class="hero is-fullheight-with-navbar {{decoClass}}">
  <div class="hero-body flex-start">
    <div class="container">
      {{> (lookup . 'template')}}
    </div>
  </div>
</section>